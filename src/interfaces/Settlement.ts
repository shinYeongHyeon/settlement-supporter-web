interface Settlement {
    settlementId: string;
    settlementDate: number;
    settlementPaidMemberId: string;
    settlementContent: string;
    settlementMemo: string;
}

export default Settlement;
